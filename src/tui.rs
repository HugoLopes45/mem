// TUI — week 2 scope. Stub for now so it compiles.

use anyhow::Result;
use std::path::PathBuf;

pub fn run_tui(_db_path: PathBuf) -> Result<()> {
    println!("TUI not yet implemented. Use `mem search <query>` for now.");
    Ok(())
}
